import { ClusterAdapterWithHeartbeat, ClusterAdapterOptions, ClusterMessage, ServerId, ClusterResponse } from "socket.io-adapter";
/**
 * Returns a function that will create a NodeClusterAdapter instance.
 *
 * @param opts - additional options
 *
 * @public
 * @see https://nodejs.org/api/cluster.html
 */
export declare function createAdapter(opts?: Partial<ClusterAdapterOptions>): (nsp: any) => NodeClusterAdapter;
export declare class NodeClusterAdapter extends ClusterAdapterWithHeartbeat {
    constructor(nsp: any, opts?: ClusterAdapterOptions);
    protected doPublish(message: ClusterMessage & {
        source: string;
    }): Promise<string>;
    protected doPublishResponse(requesterUid: ServerId, response: ClusterResponse & {
        source: string;
        requesterUid: string;
    }): Promise<void>;
}
export declare function setupPrimary(): void;
